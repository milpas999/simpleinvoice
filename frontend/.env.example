# Vite does not read the root-level .env file — copy this to frontend/.env for
# local (non-Docker) development. Docker Compose injects VITE_API_BASE_URL
# directly as a container environment variable instead.
VITE_API_BASE_URL=http://localhost:3000
